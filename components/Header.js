import { styled, Typography } from '@mui/material';

const Header = styled(Typography)({
  color: '#ffffff',
  marginBottom: '16px',
  fontFamily: 'Inter',
  fontSize: '24px',
});

export default Header;
